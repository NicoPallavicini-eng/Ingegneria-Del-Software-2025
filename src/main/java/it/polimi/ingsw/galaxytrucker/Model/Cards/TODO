TODO Cards:
CombatZoneCard: getHit(cannonballList);
EpidemicCard: process() with threads
MeteorsCard: process() with threads + getHit(MeteorList);
OpenSpaceCard: player input in enginePower
PiratesCard: getHit(cannonballList);
StarDust: process() with threads
