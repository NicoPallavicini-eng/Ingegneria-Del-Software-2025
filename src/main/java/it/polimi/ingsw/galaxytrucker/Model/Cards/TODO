TODO Cards:
CombatZoneCard:
    manage getHit(cannonballList);
    threads;
EpidemicCard:
    logic;
MeteorsCard:
    logic;
OpenSpaceCard:
    player input in enginePower;
    threads;
PiratesCard:
    manage getHit(cannonballList);
    threads;
PlanetsCard:
    player input in choosing planet;
    threads;
ShipCard:
    threads;
SlaversCard:
    threads;
SmugglersCard:
    change removeBlocks(lostBlocksNumber) with getListOfCargo();
    threads;
StationCard:
    threads;

all: connect visitor handler with threads